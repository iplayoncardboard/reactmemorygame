import React from "react";

const Header = () =>(
    <div className="header">
        <h1>Micro Brew Memory Game!</h1>
        <h2>Click on an image to earn points, but don't click on any more than once!</h2>
    </div>
)

export default Header